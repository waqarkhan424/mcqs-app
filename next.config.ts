import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */


  // tell Next.js to run the build even if ESLint finds errors\

  // eslint: {
  //   ignoreDuringBuilds: true,
  // },



};

export default nextConfig;
